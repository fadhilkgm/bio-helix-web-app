# Gemini key used server-side for transcription, image understanding and response generation.
GEMINI_API_KEY=your_gemini_api_key_here

# PostgreSQL is recommended over SQLite for realtime multi-user voice workloads.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bio_helix?schema=public

# Optional
NODE_ENV=development
